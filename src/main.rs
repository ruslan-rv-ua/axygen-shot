#![allow(dead_code)] // Remove in Task 15 when all modules are wired

mod audio;
mod capture;
mod cli;
mod clipboard;
mod config;
mod errors;
mod storage;
mod window_resolver;

fn main() {
    println!("shot: not yet implemented");
}
